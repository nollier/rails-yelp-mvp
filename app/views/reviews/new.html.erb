<div class="container">
  <h1>Add review</h1>

  <%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>

<%= link_to "Restaurant's menu", restaurants_path %>
</div>
